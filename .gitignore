# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
automation.log

# Operator overrides (IP, version, etc.) — written by the web UI / CLI.
# Deep-merged on top of config.yaml at load time. Never commit.
config.local.yaml
*.local.yaml

# Config files with sensitive data (optional - user can decide)
# config.yaml
# config-test-simple.yaml

# Debug screenshots (ignore debug PNGs)
rancher_*.png
kv_page_debug.png
*_debug.png
*_initial.png
*_no_*.png
*_error_*.png
*_save_debug.png

# Allow assets directory (images, videos, mass-import for default data)
!assets/
!assets/**/*.jpg
!assets/**/*.jpeg
!assets/**/*.png
!assets/**/*.gif
!assets/**/*.ico
!assets/**/*.webm
!assets/**/*.mp4
!assets/**/*.avi
!assets/**/*.tar

# Temporary files
*.tmp
*.bak
*.swp

# Durable run store (per-run progress/log/status for the web UI)
runs/

# Playwright / QA capture artifacts
.playwright-mcp/
qa-*.png

# Automation artifacts
# Output files from automation runs (current location: exports/, legacy: project root)
exports/
onwatch_data_export_*.yaml
onwatch_data_export_*.json
*_data_inserted_*.yaml
*_data_inserted_*.json

# Config backup files (current location: .config_backups/, legacy: project root)
.config_backups/
*.backup.*
config.yaml.backup.*

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
*.pyc
__pycache__/
